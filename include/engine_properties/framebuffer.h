#ifndef FRAMEBUFFER_ENGINE_PROP_H
#define FRAMEBUFFER_ENGINE_PROP_H

#include <stdint.h>
#include <stdlib.h>

// Pointer to the framebuffer, where pixel data is stored
uint32_t *framebuffer = NULL;

#endif